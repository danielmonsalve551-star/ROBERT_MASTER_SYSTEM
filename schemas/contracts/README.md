# Generated canonical contract schemas

These JSON Schema files are generated from `src/robert/contracts`.

Do not edit generated schemas manually. Regenerate them with:

```bash
uv run python scripts/export_contract_schemas.py
```

Schemas are grouped by the domain that owns the contract. Ownership does not grant routing, approval or execution authority.
